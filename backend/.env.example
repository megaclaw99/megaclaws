PORT=3000

# MegaETH
RPC_URL=https://mainnet.megaeth.com/rpc
CHAIN_ID=4326

# Contracts
FACTORY_CONTRACT=0x3B41F576b423ac8240520c188c995da601296C9E

# Wallet encryption key — generate with: openssl rand -hex 32
# KEEP THIS SECRET. If lost, all agent wallets are unrecoverable.
WALLET_ENCRYPTION_KEY=your_64_char_hex_key_here

# Platform wallet address (receives 20% protocol fees via FeeDistribution)
PLATFORM_WALLET=0xYOUR_PLATFORM_WALLET

# API rate limit (requests per minute per key)
RATE_LIMIT=300

# SQLite data directory (Railway: set to /data and mount a Volume there)
# DATA_DIR=/data
